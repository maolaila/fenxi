# 商户分支功能地图：BY8366-QHYL

- 商户：8366-青禾娱乐。
- 代码分支：`BY8366-QHYL`；基于演示站 `origin/BY-Demo-H5V2-PC` 的 `286514bfc` 建立。原聊天室隐藏及四栏定制已回退；最新五栏代理图标、红包弹窗与推广海报定制提交 `5a7981c86` 已推送，核对 `origin/BY8366-QHYL` 与本地 `HEAD` 均为 `5a7981c8643096f58161c1a4d8cae3f76d68e140`。
- 有效端：`h5-v2/`、`pc/`；`h5/` 已废弃。
- 主题与颜色：沿用演示站配置；未改商户 Logo、主题色或版面默认选择。截图中的金色娱乐城为 H5 可切换版面（views9）。
- 功能开关：不改后台 `CHAT_SWITCH`、`CHAT_TAB`、`ACTIVITY_VIEW`，仅在此商户分支隐藏指定前台入口。

## 定制内容

- 2026-09-21 / 重新确认的 H5 底栏：金色娱乐城（views9）保持“首页／游戏大厅／红包／代理中心／个人中心”五项及中间红包大图；聊天项替换为代理中心，灰色与金色图标使用 Figma 节点 `7:739`、`7:713` 导出的原始 SVG，点击沿用快捷菜单的登录、试玩账号和代理身份检查。红包弹窗移除“排行榜”入口及其独立弹层，保留红包、礼包、代理和客服等原有入口。影响 `h5-v2/src/views9/layout/layout.vue`、`h5-v2/src/views/home/redPacketPop.vue`、`h5-v2/src/components/HomePackPop/index.vue`。
- 2026-09-21 / 前台推广海报：H5 使用业主 `切图 (2).zip` 中 750×1334 背景和 470×470 分享图，保留动态邀请码、二维码、分享链接及商户配置 Logo；PC 使用包内 756×677 Web 背景，保留现有动态码与信息。影响 `h5-v2/src/views/personal-center/child_modal/agency_center/promotion/typeone.vue`、`pc/src/views/personalCenter/child_modal/expand/expand_me.vue` 及新增资源图。Node 14 PC/H5 生产构建通过；H5 本地金色版底栏、红包弹窗和海报在手机宽度下已视觉检查，未登录代理入口验证通过；PC 登录态海报及商户线上页面待验。提交 `5a7981c86` 已推送并核对远端引用。

- 2026-09-21 / 需求取消：撤销 8366 专属“前台聊天室隐藏／中间红包去除／底栏四功能”改动。H5 的 views2、views4、views6、views7、views8、views9 底栏及 views7 首页聊天室入口，PC 的 v3 顶部导航、v6/v8/v10 游戏导航、v10/v11 侧边栏和共享顶部导航共 14 个文件恢复为演示站对应版本；金色版底栏恢复“首页／游戏大厅／红包／聊天／个人中心”五项及中间红包大图。原有后台 `CHAT_SWITCH`、`CHAT_TAB` 等配置仍决定相应入口是否显示；签到、报表、活动分类、彩票跳转、转盘实现未回退。代码 `fa16a2e70` 已推送并核对远端，Node 14 PC/H5 生产构建与差异检查通过；线上待新版部署复验。

- 2026-09-18 / H5 金色娱乐城底栏四栏历史方案（2026-09-21 已取消）：曾按当时要求改为“首页／游戏大厅／代理中心／个人中心”，移除红包大图；提交 `14a3ebfb5`。现由 `fa16a2e70` 撤销，当前底栏恢复演示站五项。

- 2026-09-18 / H5 金色娱乐城（views9）首页“彩票”游戏平台卡片：点击整张卡片时直接进入 `/lotteryHall/ksan`，卡片内“快三”快捷文字也进入同一分类。复用彩票大厅已存在的路由参数选中逻辑；其他彩票分类、投注接口和后台配置不变。影响 `h5-v2/src/views9/home/gamesPlatForms.vue`，提交 `f5a8cd2ef` 已推送。Node 14 H5-v2 生产构建与差异检查通过；实际分类数据和登录后页面待新版部署验证。
- 2026-09-18 / H5 优惠活动“全部”居中线上复查：`https://qingheyulec.com/#/active` 的 H5 包已有 `single-category` 居中样式，但 `/api/v2/cms/discountInternational` 返回一条 `status=1` 且 `code`、`typeName` 均为空的占位分类，页面将其追加为第二个空标签，导致居中条件失效。当前分支在 views7 活动列表和活动任务版同时过滤无效分类；有效分类仍正常显示，只有“全部”时沿用居中样式。影响 `h5-v2/src/views7/active/active.vue`、`h5-v2/src/views7/active/organization.vue`，提交 `7e94d0125` 已推送。Node 14 生产构建、差异检查和线上 CMS 返回值针对性核对通过；新代码尚未部署到线上。
- 2026-09-18 / PC、H5 首页转盘浮窗：合入演示站 `3411730ac`，拖动后吸附左右边缘并保持完整显示，移除自动缩进；手动关闭、活动开关和点击进入仍沿用原逻辑。影响 `pc/src/components/LuckyWheelFloat.vue`、`h5-v2/src/components/LuckyWheelFloat.vue`；Node 14 两端生产构建通过，浏览器交互和商户环境待验。合并提交 `f78635df1` 已推送并核对远端引用。

| 端 | 功能与路径 | 处理 | 验证状态 |
| --- | --- | --- | --- |
| H5-v2 | 优惠活动 `/active`（views7 活动列表及活动任务版） | 过滤 CMS 返回的空占位分类；仅有“全部”一个有效分类时顶部居中，多分类仍可横向排列。 | 线上故障成因已核对，修复待部署复验 |
| H5-v2、PC | 个人中心签到 | 隐藏 H5 各可切换版面的签到入口、H5 底部弹层签到项，以及 PC 个人信息页签到入口；经验值仍可展示。 | H5 未登录页面无签到入口；登录后待验，后端待确认 |
| H5-v2、PC | 个人报表 `/userReport`、PC 报表 | 将公式说明固定为商户提供的文字；报表接口、金额和计算逻辑不变。 | 构建通过；登录后待验 |
| H5-v2 | 金色娱乐城（views9）底部导航 | 最新要求为“首页／游戏大厅／红包／代理中心／个人中心”五项；红包大图保留，代理图标用 Figma 导出灰／金两态；其他版面延续演示站聊天室开关行为。 | `5a7981c86` 已推送，构建和视觉验证通过，线上待复验 |
| H5-v2 | 红包弹窗与推广海报 | 弹窗不再展示排行榜；H5 海报换成青禾切图背景与分享图，动态推广信息继续由原组件生成。 | 本地检查通过，登录态分享与线上待验 |
| PC | 推广海报 | Web 海报背景换成切图包提供的青禾资源，保留动态二维码和邀请信息。 | Node 14 构建通过；登录态页面待验 |
| PC | 各版面导航及右侧快捷栏 | 取消 8366 专属聊天室隐藏，恢复演示站导航及后台开关控制。 | `fa16a2e70` 已推送；Node 14 生产构建通过，线上待复验 |
| H5-v2、PC | 首页转盘悬浮入口 | 拖动后完整吸附左右边缘，不再缩进到屏幕外；保留手动关闭和活动开关。 | 合并提交已推送，两端 Node 14 构建通过；浏览器与商户环境待验 |

## 签到机制边界

前台调用 `signUser`，实际经验值由后端签到接口发放。当前前台仓库没有控制该发放机制的实现或可验证的商户级关闭开关，因此本次采用需求指定的备选方案：隐藏签到按钮。后端是否能仅对 8366 关闭经验发放，需后端确认；本次不能声称机制已关闭。直接调用后端接口的能力也不由本次前台隐藏动作阻断。

## 本地验证

- `C:\tools\node14\node.exe` 为 `v14.21.3`；H5-v2 与 PC 生产构建均成功，`git diff --check` 通过。构建输出未纳入代码仓库。
- 本地构建页通过临时的构建产物配置切换到金色娱乐城（未改源码配置），浏览器确认四栏导航与“全部”居中；由于没有连接商户 API，活动数据、登录态报表与理财跳转仍待商户环境验证。
- 转盘功能核对：当前商户代码沿用演示站已有的 PC/H5 转盘路由、页面和首页悬浮入口，相关代码已进入上述本地构建；入口是否显示仍由 `/api/v2/turntable/list` 的活动列表、`floatEntry` 和活动 `floatOn` 决定。尚无 8366 环境，不能据此确认后台活动或权限已生效。
