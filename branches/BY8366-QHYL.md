# 商户分支功能地图：BY8366-QHYL

- 商户：8366-青禾娱乐。
- 代码分支：`BY8366-QHYL`；基于演示站 `origin/BY-Demo-H5V2-PC` 的 `286514bfc` 建立。提交 `aff3861f1a6f97f9adc05165daa90192eba209aa` 已推送并核对远端引用。后续“彩票直达快三” `f5a8cd2ef` 和“活动分类居中修复” `7e94d0125` 已在本地；代码远端 `47.52.90.110:80` 连接失败，仍待推送。
- 有效端：`h5-v2/`、`pc/`；`h5/` 已废弃。
- 主题与颜色：沿用演示站配置；未改商户 Logo、主题色或版面默认选择。截图中的金色娱乐城为 H5 可切换版面（views9）。
- 功能开关：不改后台 `CHAT_SWITCH`、`CHAT_TAB`、`ACTIVITY_VIEW`，仅在此商户分支隐藏指定前台入口。

## 定制内容

- 2026-09-18 / H5 金色娱乐城（views9）首页“彩票”游戏平台卡片：点击整张卡片时直接进入 `/lotteryHall/ksan`，卡片内“快三”快捷文字也进入同一分类。复用彩票大厅已存在的路由参数选中逻辑；其他彩票分类、投注接口和后台配置不变。影响 `h5-v2/src/views9/home/gamesPlatForms.vue`，本地提交 `f5a8cd2ef`。Node 14 H5-v2 生产构建与差异检查通过；实际分类数据和登录后页面待新版部署验证，远端推送待网络恢复。
- 2026-09-18 / H5 优惠活动“全部”居中线上复查：`https://qingheyulec.com/#/active` 的 H5 包已有 `single-category` 居中样式，但 `/api/v2/cms/discountInternational` 返回一条 `status=1` 且 `code`、`typeName` 均为空的占位分类，页面将其追加为第二个空标签，导致居中条件失效。当前分支在 views7 活动列表和活动任务版同时过滤无效分类；有效分类仍正常显示，只有“全部”时沿用居中样式。影响 `h5-v2/src/views7/active/active.vue`、`h5-v2/src/views7/active/organization.vue`，本地提交 `7e94d0125`。Node 14 生产构建、差异检查和线上 CMS 返回值针对性核对通过；新代码尚未部署到线上。

| 端 | 功能与路径 | 处理 | 验证状态 |
| --- | --- | --- | --- |
| H5-v2 | 优惠活动 `/active`（views7 活动列表及活动任务版） | 过滤 CMS 返回的空占位分类；仅有“全部”一个有效分类时顶部居中，多分类仍可横向排列。 | 线上故障成因已核对，修复待部署复验 |
| H5-v2、PC | 个人中心签到 | 隐藏 H5 各可切换版面的签到入口、H5 底部弹层签到项，以及 PC 个人信息页签到入口；经验值仍可展示。 | H5 未登录页面无签到入口；登录后待验，后端待确认 |
| H5-v2、PC | 个人报表 `/userReport`、PC 报表 | 将公式说明固定为商户提供的文字；报表接口、金额和计算逻辑不变。 | 构建通过；登录后待验 |
| H5-v2 | 金色娱乐城（views9）底部导航 | 删除中间红包大图与聊天入口，改为“首页／游戏大厅／理财／个人中心”四栏；理财仍进入原 `/moneylist`。其他可切换版面的底部及首页聊天室入口同步隐藏。 | 本地金色版面视觉复验通过；理财登录跳转待验 |
| PC | 各版面导航及右侧快捷栏 | 隐藏聊天室入口；不改后台开关或聊天室路由。 | PC Node 14 生产构建通过；浏览器待验 |

## 签到机制边界

前台调用 `signUser`，实际经验值由后端签到接口发放。当前前台仓库没有控制该发放机制的实现或可验证的商户级关闭开关，因此本次采用需求指定的备选方案：隐藏签到按钮。后端是否能仅对 8366 关闭经验发放，需后端确认；本次不能声称机制已关闭。直接调用后端接口的能力也不由本次前台隐藏动作阻断。

## 本地验证

- `C:\tools\node14\node.exe` 为 `v14.21.3`；H5-v2 与 PC 生产构建均成功，`git diff --check` 通过。构建输出未纳入代码仓库。
- 本地构建页通过临时的构建产物配置切换到金色娱乐城（未改源码配置），浏览器确认四栏导航与“全部”居中；由于没有连接商户 API，活动数据、登录态报表与理财跳转仍待商户环境验证。
- 转盘功能核对：当前商户代码沿用演示站已有的 PC/H5 转盘路由、页面和首页悬浮入口，相关代码已进入上述本地构建；入口是否显示仍由 `/api/v2/turntable/list` 的活动列表、`floatEntry` 和活动 `floatOn` 决定。尚无 8366 环境，不能据此确认后台活动或权限已生效。
